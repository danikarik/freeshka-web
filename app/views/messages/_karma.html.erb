<div id="karma">
  <button data-action="chat#upvote" data-user="<%= message.user.id %>" data-id="<%= message.id %>" class="focus:outline-none text-orange-600 font-semibold text-xs">👍🏻</button>
  <span class="text-xs"><%= message.karma %></span>
</div>

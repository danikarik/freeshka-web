<div class="gap-1 flex">
  <% post.categories.each do |category| %>
    <div class="px-2 py-1 rounded-full text-indigo-600 text-xs font-semibold border-2 border-indigo-600">
      <%= category.name %>
    </div>
  <% end %>
</div>
